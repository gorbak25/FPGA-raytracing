


// Instantiate the module
ray_sphere_normal_pipeline instance_name (
    .clk(clk), 
    .sphere_center(sphere_center), 
    .hit_point(hit_point), 
    .hit_normal_unnormalized(hit_normal_unnormalized), 
    .new_data(new_data), 
    .output_valid(output_valid)
    );


