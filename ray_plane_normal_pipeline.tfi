


// Instantiate the module
ray_plane_normal_pipeline instance_name (
    .clk(clk), 
    .plane_normal(plane_normal), 
    .hit_point(hit_point), 
    .hit_normal_unnormalized(hit_normal_unnormalized), 
    .new_data(new_data), 
    .output_valid(output_valid)
    );


